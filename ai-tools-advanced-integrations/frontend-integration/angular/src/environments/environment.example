/**
 * Environment Configuration
 *
 * CARTO and backend connection settings for the Angular frontend.
 * These values can be overridden at build time or via environment-specific files.
 */

export const environment = {
  production: false,

  // CARTO API Configuration
  apiBaseUrl: 'https://gcp-us-east1.api.carto.com',
  accessToken:
    'YOUR_ACCESS_TOKEN',
  connectionName: 'carto_dw',

  // Backend Configuration
  wsUrl: 'ws://localhost:3003/ws',
  httpApiUrl: 'http://localhost:3003/api/chat',

  // Feature flags
  useHttp: false, // Use WebSocket for chat (matches vanilla config)
};
